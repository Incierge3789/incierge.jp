---
import ProfessionalFirmsExperience from "./ProfessionalFirmsExperience.tsx";

interface Props {
    className?: string;
}

const { className = "" } = Astro.props;
---

<div class={`experience-section ${className}`}>
    <ProfessionalFirmsExperience client:visible />
</div>

---
import DocumentCollectionExperience from "./DocumentCollectionExperience.tsx";

interface Props {
    className?: string;
}

const { className = "" } = Astro.props;
---

<div class={`experience-section ${className}`}>
    <DocumentCollectionExperience client:visible />
</div>

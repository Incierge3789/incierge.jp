---
import BillingPaymentExperience from "./BillingPaymentExperience.tsx";

interface Props {
    className?: string;
}

const { className = "" } = Astro.props;
---

<div class={`experience-section ${className}`}>
    <BillingPaymentExperience client:visible />
</div>

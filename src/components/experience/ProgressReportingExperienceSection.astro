---
import ProgressReportingExperience from "./ProgressReportingExperience.tsx";

interface Props {
    className?: string;
}

const { className = "" } = Astro.props;
---

<div class={`experience-section ${className}`}>
    <ProgressReportingExperience client:visible />
</div>
